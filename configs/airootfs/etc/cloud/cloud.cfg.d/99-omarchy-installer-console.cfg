# The installer dashboard owns the live environment's system console.
# Keep cloud-init provisioning enabled, but do not print SSH key material over it.
no_ssh_fingerprints: true
ssh:
  emit_keys_to_console: false
