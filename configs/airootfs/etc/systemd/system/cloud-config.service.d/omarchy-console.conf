[Service]
# The install dashboard owns tty1. Keep this stage shim's completion or failure
# message in the journal so it cannot overwrite the finished installer screen.
StandardOutput=journal
StandardError=journal
