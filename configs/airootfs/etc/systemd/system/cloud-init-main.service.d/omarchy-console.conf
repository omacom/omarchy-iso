[Service]
# The install dashboard owns tty1. Keep cloud-init diagnostics in the journal
# so its final SSH-key messages cannot overwrite a fast install's finish screen.
StandardOutput=journal
StandardError=journal
