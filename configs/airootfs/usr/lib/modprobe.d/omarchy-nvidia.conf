# Enforce NVIDIA kernel modesetting (+ DRM framebuffer) on the Omarchy live
# medium. Merged with the driver's own drop-in; setting the same options here
# makes the live boot deterministic. nvidia_drm only loads against the stock
# linux kernel, which is the only boot that carries the nvidia modules.
options nvidia_drm modeset=1 fbdev=1
